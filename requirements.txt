# policy-sop-assistant requirements
# Pin major versions; use pip-compile for reproducible lockfile

# Cloud Functions framework
functions-framework==3.*

# GCP clients
google-cloud-storage==2.*
google-cloud-discoveryengine==0.11.*
google-cloud-logging==3.*
google-cloud-firestore==2.*       # Issue #2: Firestore session persistence
google-auth==2.*                  # Issue #1: Google Identity token verification
google-auth-oauthlib==1.*
google-api-python-client==2.*     # Issue #1: Admin SDK for group membership

# Vertex AI / Gemini
google-cloud-aiplatform==1.*

# FastAPI (Cloud Run API)
fastapi==0.111.*
uvicorn[standard]==0.29.*
pydantic==2.*

# Document parsers
pymupdf==1.*            # PDF  -> fitz
python-docx==1.*        # DOCX
beautifulsoup4==4.*     # HTML
lxml==5.*               # HTML parser backend

# Diff engine (Issue #3)
diff-match-patch==20230430  # Word-level policy version diff

# Caching (Issue #1: Redis group membership cache)
redis==5.*

# Utilities
python-dotenv==1.*
tenacity==8.*

# Testing
pytest==8.*
pytest-asyncio==0.23.*
httpx==0.27.*          # FastAPI test client
